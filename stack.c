#include "monty.h"

/**
 * f_stack - the entry point.
 * Description - prints the top.
 * @head: the stack head.
 * @counter: the line number.
 * Return: .
 */

void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
