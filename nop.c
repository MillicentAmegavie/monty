#include "monty.h"

/**
  *f_nop- the entry point.
  *Description - nothing.
  *@head: the stack head.
  *@counter: the line number.
  *Return: .
 */

void f_nop(stack_t **head, unsigned int counter)
{
	(void) counter;
	(void) head;
}
